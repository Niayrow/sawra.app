import { SEO_HOME } from '../../utils/seo';
import { seoDocToMetadata } from '../../utils/seoMetadata';

export const metadata = seoDocToMetadata(SEO_HOME);

export default function HomePage() {
  return null;
}
